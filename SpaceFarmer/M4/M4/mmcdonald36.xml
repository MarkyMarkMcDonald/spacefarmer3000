<!-- All our projects begin with the project tag -->
<project name="M4 Ant" default="all" basedir=".">

	<!-- our init target is a prereq for everything -->
	<target name="init">
		<property name="src.dir" value="src"/>
		<property name="build.dir" value="build"/>
		<property name="api.dir" value="api" />
		<property name="lib.dir" value="lib/resources.jar" />
		<property name="mainClass" value="edu/gatech/oad/antlab/pkg1/AntLabMain"/>

	</target>

	<!-- prepare is a prereq for most tasks except cleanup     -->
	<target name="prepare" depends="init">
		<mkdir dir="${build.dir}"/>
		<mkdir dir="${api.dir}" />
	</target>

	<!-- compile the code -->
	<target name="compile" depends="prepare">
		<javac srcdir="${src.dir}" destdir="${build.dir}"> 
			<classpath>
				<pathelement location="${lib.dir}" />
			</classpath>
		</javac>
	</target>

	<!-- create the javadoc -->
	<target name="javadoc" depends="compile">
		<javadoc sourcepath="${src.dir}" destdir="${api.dir}">
			<classpath>
				<pathelement location="${lib.dir}" />
			</classpath>
		</javadoc>
	</target>

	<!-- Create a jar -->
	<target name="jar" depends="compile">
		<jar destfile="AntLabRun.jar" basedir="${build.dir}">
			<manifest>
				<attribute name="Class-Path" value="${lib.dir}" />
				<attribute name="Main-Class" value="${mainClass}" />
			</manifest>
		</jar>
	</target>


	<!-- run the application -->
	<target name="run" depends="jar">
		<java fork="true" jar="AntLabRun.jar">
			<classpath>
				<pathelement location="${mainClass}"/>
				<pathelement location="${lib.dir}" />
			</classpath>
		</java>
	</target>


	<!-- clean up all files generated by the build -->
	<target name="clean" depends="init">
		<delete dir="${build.dir}"/>
		<delete dir="${api.dir}" />
		<delete file="AntLabRun.jar" />
	</target>

	<!-- run all tasks except cleanup -->
	<target name="all" depends="run,javadoc"/>
</project>
